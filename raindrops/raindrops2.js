// const CONVERSIONS = {  3:'Pling', 5:'Plang', 7:'Plong'}

// export default () => ({
//   convert: (num) => {
    
//     //DESTRUCTURING
//     // return Object.entries(CONVERSIONS).map(([val, sound]) => {
//     //   console.log([val,sound])
//     //   if (num % val === 0) return sound;
//     // }).join('') || '' + num;
    
//     //OLDSCHOOL
//     // return Object.entries(CONVERSIONS).map((x) => {
//     //   console.log(x)
//     //   if (num % x[0] === 0) return x[1];
//     // }).join('') || '' + num;
//   }
// })